Place your provided PNG icon here as `icon.png` so the PWA will use it.

Path: `public/pwa/icon.png`

Instructions:
1. Download the PNG you attached in the chat.
2. Save it to `public/pwa/icon.png` (overwrite if present).
3. The app manifest and layout already reference `/pwa/icon.png`; no further code changes are required.

If you'd like, I can save the file for you if you re-attach it as a workspace file or allow access to the attachment via upload dialog.